import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function TransactionsPage() {
  return (
    <PagePlaceholder
      title="Transactions"
      description="Filter, split, categorize, and export transactions synced from your linked accounts."
    />
  );
}
