import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Decimal-place rounding, connected accounts, and profile settings."
    />
  );
}
