import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="Spending by category, cash inflows/outflows, account balances, source balances, float outstanding, and reimbursements land here."
    />
  );
}
