import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function SourcesPage() {
  return (
    <PagePlaceholder
      title="Sources"
      description="Manage your buckets — Current Budget, Advance, Reimbursement, sinking funds — and their present/future balances."
    />
  );
}
