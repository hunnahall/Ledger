import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function AccountsPage() {
  return (
    <PagePlaceholder
      title="Accounts"
      description="Connect bank accounts via Teller and manage sync status."
    />
  );
}
